# VBench Motion Smoothness runtime dependencies used by the official vbench==0.1.5 wheel.
# Install with:
#   python -m pip install -r requirements/metrics-motion-runtime.txt
#   python -m pip install --only-binary=:all: --no-deps vbench==0.1.5
# Install torch/torchvision separately for the target CUDA and hardware.
# Versions below are from the validated Ali02 environment; this is not a full lock file.
numpy==2.2.6
Pillow==11.3.0
opencv-python-headless==4.11.0.86
tqdm==4.69.0
omegaconf==2.3.0
decord==0.6.0
imageio==2.37.4
# The official CLI imports static_filter while registering subcommands;
# its bundled RAFT utilities import scipy even for motion_smoothness.
scipy==1.17.1
